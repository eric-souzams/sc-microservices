server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/eric-souzams/sc-config-server
          default-label: main
          search-paths:
          - 'greeting-service*'